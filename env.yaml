# Add any environment variables (such as paths to dataset files etc.) to this .yaml file, load them in env.py and
# then use them via `from env import VARIABLE_NAME`

dataset_dir: ''
model_saves_dir: ''
logs_dir: ''
plots_dir: ''
